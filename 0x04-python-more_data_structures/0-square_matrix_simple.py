#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    if (len(matrix > 0)):
        new_list=[]
        for i in matrix:
            newlist.append(list(map(lambda x : x**2,i)))
            return new_list
                
